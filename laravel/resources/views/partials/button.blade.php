<div class="button-container">
  <button 
    class="button-container__btn button-container__btn-js"
    id="{{$id}}"
    data-stat="{{ $stat }}"
    data-type="{{ $type }}"
    aria-label="text">
  </button>

  <span class="button-container__text">{{ $id }}</span>
</div>